# `image/` — 사진 **원본** 보관함

이 폴더는 **사이트가 직접 쓰지 않습니다.** 화면에 나가는 사진은 `public/`
아래에 있는 줄인 파일들입니다 (머리띠는 `public/header/`, 본문에 들어가는
사진은 `public/about/`).

여기에는 그 원본(크고 무거운 파일)을 남겨 둡니다. 나중에 사진을 다시
자르거나 다른 크기로 뽑아야 할 때 원본이 없으면 처음부터 사진을 다시
구해야 하기 때문입니다.

## 지금 들어 있는 것

| 원본 | 크기 | 줄여서 넣은 곳 | 쓰는 화면 |
|---|---|---|---|
| `modi.jpg` | 5116×3410, 1.7MB | `public/header/modi.jpg`, `modi-sm.jpg` | 종목 안내 |
| `나침반.jpg` | 3360×2004, 1.3MB | `public/header/compass-map.jpg`, `compass-map-sm.jpg` | (지금은 안 씀) |
| `일정2.jpg` | 1646×812, 0.33MB | `public/header/build.jpg`, `build-sm.jpg` | 일정 |
| `일정.JPG` | 1647×561, 0.16MB | `public/header/front-desk.jpg`, `front-desk-sm.jpg` | 자주 묻는 질문 |
| `오는길.JPG` | 1637×539, 0.25MB | `public/header/entrance.jpg`(1600×378), `entrance-sm.jpg`(900×213) | 오시는 길 **(아래를 잘라서 넣음)** |
| `질문.jpg` | 7296×4225, 3.1MB | `public/header/question.jpg`, `question-sm.jpg` | (지금은 안 씀) |
| `수상.jpg` | **836×394**, 0.13MB | `public/header/awards.jpg` **(한 장만)** | 참가 신청 |
| `트로피.jpg` | 2132×1107, 0.62MB | `public/header/trophies.jpg`, `trophies-sm.jpg` | (지금은 안 씀) |
| `참가신청.jpg` | 5026×1910, 1.1MB | `public/header/apply.jpg`, `apply-sm.jpg` | (지금은 안 씀) |
| `ROBOFEST about image.png` | 1371×915, 1.7MB | `public/header/robot-table.jpg`, `robot-table-sm.jpg` | ROBOFEST 소개 |
| `로봇 손 사람 손.jpg` | 2160×2700, 0.9MB | `public/header/hands.jpg`, `hands-sm.jpg` | (지금은 안 씀) |
| `상장.jpg` | 2131×1422, 0.53MB | (아직 안 넣었음) | (아직 안 씀) |
| `단체.jpeg` | 886×591, 0.11MB | (아직 안 넣었음) | (아직 안 씀) |
| `대회장.jpg` | 4897×3266, 3.1MB | `public/about/hall.jpg`, `hall-sm.jpg` | ROBOFEST 소개 **(머리띠 아님, 본문 사진)** |
| `과기대 사진.jpg` | 400×238, 0.11MB | `public/venue/campus.jpg` | 장소 **(머리띠 아님, 본문 사진)** |
| `부산과학기술대학교.jpg` | 960×640, 0.3MB | (지금은 안 씀) | 장소 — **2026-08-12 이전** 사진 |
| `과기대 체육관 지도.png` | 1010×648, 0.45MB | `public/venue/map.png` | 장소 — 지도 **(사진 아님, 네이버 지도 캡처)** |

> ℹ️ **`ROBOFEST about image.png` 는 잘라서 넣었습니다 (2026-08-12).**
> 원본 1371×915 에서 위 70px(바닥·사람 다리가 찍힌 어두운 띠)과 아래
> 115px(빈 파란 탁자보)을 잘라 **1371×730** 으로 만들었습니다. 담당자
> 요청이 '탁자 위 로봇에 초점'이라 로봇이 있는 가로 띠만 남긴 것입니다.
> 머리띠가 아주 납작해서(3.86:1) 세로로 절반쯤만 보이므로,
> `config` 의 `headerImages.robotTable` 에서 `position: center 42%` 로
> 로봇 몸통이 한가운데 오도록 맞춰 두었습니다.
> 다른 머리띠 사진과 달리 원본이 1371px 뿐이라 `wide` 도 1600 이 아니라
> **1371** 입니다. 없는 화소를 늘려 봐야 뿌옇기만 합니다.

> ⚠️⚠️ **`과기대 사진.jpg` 는 400×238 로 아주 작습니다 (2026-08-12).**
> 이 크기가 화면에 나오는 크기의 한계를 정합니다. 지금은 **360px** 폭으로
> 줄여서 보여 주고 있어 또렷합니다 — 원본보다 크게만 안 하면 됩니다.
> **⚠️ 400px 보다 크게 보여 주지 마세요. 늘리는 순간 흐릿해집니다.**
> (처음에는 730px 로 깔았다가 흐려서 줄였습니다)
> 큰 사진이 생기면 `public/venue/campus.jpg` 를 바꾸고
> `config/competition.ts` 의 `venue.photo` 에서 `width`·`height` 도
> **실제 크기로** 고치세요.
>
> 이 사진도 지도처럼 **한 장만** 만듭니다. 400px 을 더 줄일 이유가 없어
> `campus-sm.jpg` 는 없앴습니다. 나중에 큰 사진을 넣게 되면 그때 다시
> 두 장으로 나누고 `venue/page.tsx` 의 `srcSet` 도 되살리세요.
>
> ℹ️ **`부산과학기술대학교.jpg` (960×640) 는 그 전에 쓰던 사진입니다.**
> 지금은 화면에 나가지 않지만, 새 사진보다 크기가 커서 남겨 두었습니다.
> 되돌리고 싶으면 이 파일을 960px 로 뽑아 `public/venue/campus.jpg` 에
> 넣고 `venue.photo` 의 `width`·`height` 를 960·640 으로 고치면 됩니다.

> ℹ️ **`과기대 체육관 지도.png` 는 다른 것과 규칙이 다릅니다 (2026-08-12).**
> ① **한 장만 만듭니다.** 다른 사진은 큰 것·작은 것 두 장인데, 지도는
>    작게 줄이면 글씨가 뭉개져 읽을 수 없게 됩니다. 화면에 나오는 크기가
>    가장 넓어야 672px 이라 1010px 한 장이면 휴대폰까지 충분합니다.
> ② **아래쪽 흰 띠 56px 을 잘라냈습니다.** 캡처할 때 네이버 지도 화면
>    아래 여백이 같이 찍힌 것입니다. 그대로 두면 지도 상자 안에 흰 칸이
>    남아 고장난 것처럼 보입니다. 그래서 648 → **592** 로 잘랐습니다.
> ③ **png 그대로 둡니다.** 지도는 글씨와 선이 많아 jpg 로 바꾸면 글자
>    둘레가 지저분해집니다. 색을 256가지로 줄여 450KB → 188KB 가 됐고,
>    2배로 확대해 비교해도 차이가 보이지 않았습니다.
>
> 다시 만들려면: 원본을 `image/` 에 넣고 아래쪽 흰 여백을 잘라낸 뒤
> `public/venue/map.png` 로 저장하고, `config/competition.ts` 의
> `venue.mapImage` 에서 `width`·`height` 를 **실제 크기로** 고치세요.
> (크기를 틀리게 적으면 그림이 뜰 때 아래 글이 덜컥 밀립니다)

> ⚠️ **`대회장.jpg` 는 세계대회 현장 사진입니다. 부산 국내예선 사진이
> 아닙니다.** 그래서 화면에 '지난 ROBOFEST 세계대회 현장'이라는 설명이
> 함께 나갑니다. 사진을 부산 사진으로 바꾸게 되면
> `config/competition.ts` 의 `aboutPage.photo.caption` 도 같이 고치세요.

> ⚠️ **`오는길.JPG` 도 세계대회(LTU) 현장 사진입니다 (2026-08-12).**
> 건물 입구에 'ROBOFEST World Championship / MAY 15~17' 현수막이 걸려
> 있고 벽에 LTU 그림이 있습니다. 부산 대회장 입구가 아닙니다.
> `대회장.jpg` 와 달리 이것은 **머리띠**라서 설명을 함께 붙일 자리가
> 없습니다 (머리띠에는 제목만 올라갑니다).
> 남색 막에 덮이는 분위기용 배경이지만 현수막 글씨는 읽히므로, 부산
> 대회장 사진이 생기면 이 자리를 그것으로 바꾸는 것이 낫습니다.
>
> ℹ️ **같은 날 `오시는길.JPG`(1647×709) 를 먼저 넣었다가 이 사진으로
> 바꿨습니다.** 담당자가 그 파일을 `image/` 폴더에서 지웠으니 되돌릴
> 원본은 없습니다. `public/header/entrance.jpg` 라는 **이름은 그대로**
> 두고 안의 사진만 갈았습니다 — 이름을 바꾸면 `config` 와 화면 코드까지
> 따라 고쳐야 해서 그렇게 했습니다.
>
> ⚠️⚠️ **`오는길.JPG` 는 아래를 잘라서 넣었습니다 (2026-08-12).**
> 원본 539 높이에서 **아래 152px(잔디·길 앞부분)을 잘라 387** 로 만든 뒤
> 1600px·900px 두 장으로 줄였습니다. 담당자 요청이 **'노란 ROBOFEST 글자가
> 화면에 보이되 가장 하단에'** 였기 때문입니다.
> **자른 자리는 노란 글자의 마지막 줄입니다** (글자 아래 여백 0px).
>
> **`position` 값만으로는 안 되는 일이었습니다.** 원본에서는 글자 아래로
> 잔디·길이 152px 더 있어서, 1440px 화면에서는 아무리 내려도 글자 아래에
> 61px 이 남았습니다. 그래서 사진 자체를 자르고 세로를 `100%`(아래쪽에
> 맞춤)로 두었습니다.
> **아래쪽에 맞추면 사진의 맨 아랫줄은 어느 화면에서도 반드시 보입니다**
> (잘리는 것은 언제나 위쪽입니다). 그 맨 아랫줄이 곧 글자의 마지막 줄이라,
> 어느 화면 폭에서도 글자가 띠 밑변에 정확히 닿고 잘리지도 않습니다.
>
> ⚠️ **되돌리지 마세요.** 원본 전체로 다시 만들면 글자가 밑변에서
> 떨어집니다. 세로를 `50%` 로 바꾸는 것도 같은 결과입니다.
>
> ℹ️ **가로는 `5%` 입니다.** 자르면서 비율이 3.04:1 → **4.23:1** 로 더
> 납작해져 약 1672px 보다 좁은 화면에서는 좌우가 잘립니다. 노란 글자가 사진
> 왼쪽 끝에서 42px 밖에 안 떨어져 있어서, 가로를 올리면 'R' 부터 잘립니다.
> 대신 오른쪽(자전거 옆)이 빠집니다. **이 값을 올리지 마세요.**

> ★★★ ⚠️ **`일정2.jpg` — 학생 얼굴이 나옵니다. 초상권을 확인하세요** ⚠️ ★★★
> (2026-08-12)
>
> 이 사진에는 **학생(미성년자)의 옆얼굴이 알아볼 수 있게** 나옵니다.
> 머리띠는 남색 막(0.76)에 덮이지만 얼굴 윤곽은 그대로 읽힙니다.
> 주최가 교육청인 행사 누리집이므로, **사진 속 학생(과 보호자)의 동의가
> 있는 자료인지 담당자가 확인해야 합니다.** 확인이 안 되면 둘 중 하나로
> 가세요.
> ① 오른쪽(학생 얼굴 쪽)을 잘라낸 사진으로 다시 만들기 — 주인공인 로봇과
>    손은 가운데·왼쪽에 있어 사진이 상하지 않습니다.
> ② 아래 `일정.JPG`(얼굴이 안 보이는 사진)로 되돌리기.
>
> ⚠️ 넓은 화면에서는 좌우가 잘리지 않습니다. 그래서 `position` 값으로는
> 얼굴을 화면 밖으로 밀어낼 수 없습니다 — **사진 파일 자체를 잘라야
> 합니다.**
>
> ℹ️ 막이 0.76 인 이유는 아래 '막 진하기를 정할 때' 를 보세요.
> ℹ️ 레고(EV3)가 나오지만 Robofest 는 'Any Kit' 입니다. '레고 대회'로
> 오해하지 않도록 종목 안내 글에서 키트 제한이 없다는 점을 계속 밝혀 두세요.

> ℹ️ **`일정.JPG` 는 2026-08-12 하루에 자리를 두 번 옮겼습니다** (모두
> 담당자 요청). 이름이 '일정'인데 **지금은 '자주 묻는 질문' 화면에**
> 쓰입니다 — 이름과 쓰이는 곳이 다르니 헷갈리지 마세요.
> ① 아침에 '일정'(/schedule) 에 넣음 → ② 곧 '일정' 은 `일정2.jpg` 로 갈림
> → ③ 같은 날 '자주 묻는 질문'(/faq) 으로 옮김.
> ③의 이유는 ①과 같습니다: **초록색 가방의 ROBOFEST 글자가 보이게** 해
> 달라는 요청.
>
> 이 사진은 사람이 여럿 나오지만 **위에서 내려다보고 찍어 얼굴이 거의
> 보이지 않습니다.** 그래서 초상권 문제가 `일정2.jpg` 보다 가볍습니다.
>
> ⚠️ **`position` 은 `center 100%`(아래쪽에 맞춤) 입니다.** 초록색 가방이
> 사진 맨 아래에 있어서, 가운데(50%)로 두면 넓은 화면에서 가방 글씨가
> 잘립니다. **바꾸지 마세요.** (가방 글씨는 원본에서 이미 잘려 있어, 값으로는
> 온전한 글씨가 나오지 않습니다)

> ℹ️ **`상장.jpg` 와 `단체.jpeg` 는 담당자가 2026-08-12 에 폴더에 넣어 둔
> 사진입니다. 아직 어느 화면에도 쓰지 않습니다.** 화면에 쓰려면 이 문서 맨
> 아래 '사진을 바꾸려면' 순서대로 두 크기로 줄여 `public/header/` 에 넣고
> `config` 의 `headerImages` 에 항목을 만드세요.
> ⚠️ `단체.jpeg` 는 886×591 로 작습니다. 머리띠로 쓰면 `수상.jpg` 처럼
> 넓은 화면에서 흐려집니다 (아래 설명 참고). 사람 얼굴이 나오는 사진이라면
> 초상권도 함께 확인하세요.

> ★★★ ⚠️⚠️ **`수상.jpg` 는 836×394 로 작습니다 (2026-08-12)** ⚠️⚠️ ★★★
> ('참가 신청' 머리띠)
>
> 머리띠 사진 중 유일하게 권장 크기(가로 1600px)의 **절반**입니다.
> 1440px 화면에서 **1.72배**, 1920px 화면에서 **2.3배**로 늘려 보여 주므로
> 그만큼 흐립니다. 남색 막(0.76)이 상당히 가려 주지만 큰 모니터에서는
> 보입니다. **없는 화소는 만들 수 없습니다** — `과기대 사진.jpg`(400px) 와
> 같은 사정이고, 더 큰 원본을 구하는 것 말고는 방법이 없습니다.
> 큰 사진이 생기면 `public/header/awards.jpg` 를 1600px 로 다시 만들고
> `config` 의 `awards.small` 을 900px 짜리 새 파일로 나누세요.
>
> ℹ️ **그래서 파일이 한 장입니다** (`wide`·`small` 이 같은 파일). 원본이
> 836px 뿐이라 나눌 까닭이 없고, 82KB 로 머리띠 중 가장 가벼워 휴대폰이
> 그대로 받아도 부담이 없습니다. 오히려 **휴대폰에서 가장 또렷합니다** —
> 좁은 화면은 세로에 맞춰 넣어 늘릴 필요가 없기 때문입니다.
>
> ⚠️ **'상' 이야기를 하는 사진입니다.** 2025년 세계대회(LTU) 시상식이고 우리
> 국내예선의 시상이 아닙니다. 벽에 LTU 현수막, 위쪽에 미국 국기가 있습니다.
> `position: center 70%` 이 그 국기를 거의 밀어냅니다 — **값을 내리지
> 마세요.** 아래 `트로피.jpg` 항목의 문구 주의도 그대로 적용됩니다.
>
> ℹ️ 관중석에 학생이 수백 명 나오지만 한 사람이 몇 픽셀이라 얼굴을 알아볼 수
> 없습니다. `일정2.jpg` 와 달리 초상권 문제는 가볍습니다.

> ⚠️ **`트로피.jpg` 는 2026-08-12 넣었다가 같은 날 `수상.jpg` 로 갈렸습니다.**
> 둘 다 담당자 요청입니다. **다른 사진입니다** — `트로피.jpg` 는 트로피
> 근접, `수상.jpg` 는 체육관 시상식 전경. 파일과 `config` 의 `trophies`
> 항목은 남겨 두었습니다. 되돌리려면 `/apply` 의 `page.tsx` 에서
> `headerImages.trophies` 로 바꾸면 됩니다.
>
> 아래는 그때 적어 둔 문구 주의입니다. **`수상.jpg` 에도 그대로 적용됩니다.**

> ★★★ ⚠️ **`트로피.jpg` — '상' 이야기를 하는 사진입니다** ⚠️ ★★★
> (2026-08-12)
>
> 트로피 받침 명패에 **`1st Place Senior UMC` · `2nd Place Junior UMC` ·
> `2025 ROBOFEST World Championship`** 이 적혀 있습니다. **2025년
> 세계대회(LTU)의 시상 트로피이고, 우리 국내예선의 상이 아닙니다.**
>
> `CLAUDE.md` 의 문구 규칙과 부딪히지 않게 두 가지를 지켜 주세요.
> ① 출전권은 **'기회'로만** 씁니다 — '상위팀에 세계대회 진출 기회 제공'.
>    진출 팀 수는 아직 정해지지 않았으니 숫자를 쓰지 마세요.
> ② 이 사진 때문에 '참가하면 트로피를 받는다'로 읽히지 않도록, `/apply`
>    본문에서 시상 이야기를 새로 만들지 마세요.
>
> ℹ️ `position: center 30%` 이 넓은 화면에서는 명패를 화면 밖으로 밀어냅니다
> (컵과 윗부분만 보입니다). **좁은 화면에서는 세로가 잘리지 않아 명패가
> 들어옵니다** — 글씨가 작고 막에 덮이지만 남아 있습니다. 어느 화면에서도
> 안 보이게 하려면 사진 아래쪽을 잘라야 합니다.
>
> ℹ️ 뒤쪽 사람들은 모두 흐릿해 얼굴을 알아볼 수 없습니다 — `일정2.jpg` 와
> 달리 초상권 문제는 가볍습니다.
>
> ⚠️ **머리띠 사진 중 가장 무겁습니다 (wide 243KB).** 금빛 반사와 흐린
> 배경이 많아 jpg 가 잘 줄지 않습니다. 이보다 더 무거운 사진은 넣지 마세요.

> ⚠️ **`참가신청.jpg`(`apply`) 는 2026-08-12 부터 쓰는 곳이 없습니다.**
> '참가 신청' 화면이 위의 `트로피.jpg` 로 갈렸습니다. **이름에 낚이지
> 마세요 — `apply` 사진은 이제 그 화면이 쓰지 않습니다.**
> 파일과 `config` 항목은 남겨 두었습니다. 되돌리려면 `/apply` 의
> `page.tsx` 에서 `headerImages.apply` 로 바꾸면 됩니다.
> ⚠️ 그 사진의 막 0.68 은 **어두운 사진이라** 낮게 둘 수 있었던 값입니다.
> `trophies` 에 옮겨 쓰면 설명 줄이 규정에 걸립니다.

> ⚠️ **`나침반.jpg`(`compass-map`) 는 2026-08-12 부터 쓰는 곳이 없습니다.**
> 그날 '오시는 길'과 '일정' 두 화면이 차례로 다른 사진으로 갈렸습니다.
> 파일과 `config` 항목은 지우지 않고 남겨 두었습니다 — 되돌리려면 그
> 화면의 `page.tsx` 에서 `headerImages.compassMap` 으로 바꾸면 됩니다.

`public/header/` 에는 원본이 이 폴더에 없는 사진도 있습니다
(`robot`). 예전에 넣은 것으로, 원본을 따로 보관하지 않았습니다.

> ⚠️ **`compass`(나무 탁자 위의 황동 나침반) 는 2026-08-14 에 지웠습니다.**
> 종목 상세 화면 머리띠가 사진에서 **남색 단색**으로 바뀌면서(담당자 요청)
> 쓰는 곳이 없어졌습니다. `public/header/compass.jpg`·`compass-sm.jpg` 와
> `config` 의 `headerImages.compass` 항목을 함께 지웠습니다.
> **이 폴더에 원본이 없던 사진이라, 되살릴 곳은 git 기록뿐입니다.**
> (`git log --diff-filter=D -- public/header/compass.jpg`)
> ⚠️ 이름이 비슷한 `compass-map`(`compassMap`) 은 **다른 사진**이고
> 파일도 항목도 그대로 있습니다.

> ⚠️ `robot` 은 **2026-08-12 부터 쓰는 곳이 없습니다.**
> 2026-08-06 에 참가 신청 화면에서 빠졌고, 08-12 에 ROBOFEST 소개
> 화면마저 `robot-table`(위 표의 `ROBOFEST about image.png`)로 바뀌었습니다.
> 파일은 지우지 않고 남겨 두었습니다 — 되돌리려면 `config` 의
> `headerImages.robotTable` 자리를 쓰는 `about/page.tsx` 에서
> `headerImages.robot` 으로 바꾸면 됩니다.
> ⚠️ 이름이 비슷한 `robot` 과 `robot-table` 은 **서로 다른 사진**입니다.

## 막(overlay) 진하기를 정할 때

사진마다 `config/competition.ts` 의 `headerImages` 에 `overlay` 값이 있습니다.
**사진이 밝을수록 큰 값**입니다. 정할 때 두 가지를 꼭 같이 보세요.

1. **평균 밝기가 아니라 '가장 밝은 점'을 보세요.** 어두운 사진에도 밝은
   점(조명·흰 물체·빛나는 선)이 있으면 그 점 위에서 글씨가 사라집니다.
2. **제목보다 그 아래 설명 줄이 더 까다롭습니다.** 제목은 흰색·큰 글씨라
   3:1 만 넘으면 되지만, 설명 줄은 옅은 파랑(brand-100)에 작은 글씨라
   **4.5:1** 이 필요합니다. 제목만 보고 정하면 설명 줄이 깨집니다.

지금 값: `robotTable` 0.72 · `modi` 0.72 ·
`build` 0.76 · `entrance` 0.68 · `frontDesk` 0.76 · `awards` 0.76 ·
`apply` 0.68 (지금은 쓰는 곳 없음) ·
`compassMap` 0.68 (지금은 쓰는 곳 없음) ·
`question` 0.76 (지금은 쓰는 곳 없음) ·
`trophies` 0.76 (지금은 쓰는 곳 없음) ·
`robot` 0.72 (지금은 쓰는 곳 없음) · `hands` 0.72 (지금은 쓰는 곳 없음).

> ⚠️ **`awards`(참가 신청) 의 0.76 은 0.72 아래로 내리지 마세요 (2026-08-12).**
> 체육관 조명·흰 벽·금색 트로피 때문에 글자 칸의 가장 밝은 점이 거의
> 순백(1.0)입니다. 0.68 → 설명 줄 4.30:1 **미달** · 0.72 → 4.91:1 겨우 통과
> · **0.76 → 5.61:1**. 사진이 흐린 것을 가려 주는 효과도 있습니다.
> (같은 날 잠깐 썼던 `trophies` 도 같은 이유로 0.76 이었습니다)
> **바꾸기 전 `apply` 사진의 0.68 을 그대로 가져오면 '접수 기간' 줄이
> 규정에 걸립니다** — 그 값은 어두운 사진에 맞춘 것이었습니다.

> ⚠️ **`build`(일정) 의 0.76 은 0.72 아래로 내리지 마세요 (2026-08-12).**
> 글자가 놓이는 칸에 흰 종이와 투명 물병이 있어 가장 밝은 점이 순백(1.0)
> 입니다. 0.72 → 설명 줄 4.90:1 (겨우 통과) · **0.76 → 5.60:1**.
> 세로 자리를 바꿔도 가장 밝은 점은 그대로 순백이라 이 값은 달라지지 않습니다.

> ⚠️ **`frontDesk`(자주 묻는 질문) 의 0.76 도 낮추지 마세요 (2026-08-12).**
> 사진 전체는 어두운데(평균 밝기 0.34) **설명 줄이 놓이는 자리에 하얀
> 인쇄물이 깔려 있습니다.** 거의 순백(1.0)이라 평균만 보고 정하면 깨집니다.
> 설명 줄이 실제로 덮는 칸을 기준으로 재면
> 0.68 → 4.30:1 **미달** · 0.72 → 4.90:1 겨우 통과 · **0.76 → 5.60:1**.
> 글월 길이나 화면 폭이 바뀌면 종이의 다른 부분이 글자 밑으로 들어오므로
> 여유를 두었습니다. 사진을 더 보이게 하려면 0.72 까지가 한계입니다.
> (`/faq` 로 옮긴 뒤 다시 재도 제목 7.09:1 · 설명 줄 5.61:1 로 같습니다)
>
> ℹ️ 여기가 이 문서의 1번 규칙('평균이 아니라 가장 밝은 점')이 실제로
> 걸린 자리입니다. 평균 0.34 만 보면 0.58~0.68 을 골랐을 사진입니다.

> ℹ️ **`entrance`(오시는 길) 의 0.68 은 설명 줄이 4.96:1 로 통과합니다.**
> 쓰지 않게 된 `compassMap` 도 같은 0.68 이었지만 그쪽은 4.30:1 로
> 미달이었습니다 (2026-08-06 에 발견, 고치기 전에 안 쓰게 됐습니다).
> **값이 같아도 사진이 달라 결과가 다릅니다** — 한쪽 값을 다른 쪽에 그대로
> 옮겨 쓰지 마세요.

## 사진을 바꾸려면

**⚠️ 원본을 `public/` 에 그대로 올리지 마세요.** 1.7MB짜리 사진을 휴대폰에서
내려받게 되어 화면이 늦게 뜹니다. 반드시 두 크기로 줄여서 넣습니다.

1. 새 원본을 이 폴더에 넣습니다.
2. 가로 **1600px** 와 **900px** 두 장으로 줄여
   `public/header/이름.jpg` 와 `public/header/이름-sm.jpg` 로 저장합니다.
   (줄이는 도구는 무엇이든 됩니다 — 그림판, 미리보기, 온라인 도구)
3. `config/competition.ts` 의 `headerImages` 에서 그 이름을 가리키게 합니다.
   각 항목 위에 한국어 설명이 붙어 있습니다.

`wide`(1600px)는 640px 이상 넓은 화면에서, `small`(900px)은 그보다 좁은
화면에서 쓰입니다. 나뉘는 규칙은 `src/app/globals.css` 의 `.header-photo` 에
있습니다.
